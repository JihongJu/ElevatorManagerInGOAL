init module{
	knowledge{
	}
	beliefs{
	}
	goals{
	}
}

main module{
	program {
		% receive message from "elevator"
		if bel( received(elevator, atFloor(Level)) ) then insert(people(3)) + (elevator).sendonce((people(3))).
	}
}
