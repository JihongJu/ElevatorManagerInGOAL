init module{
	knowledge{
	}
	beliefs{
	}
	goals{
	}
}

main module{
	program {
		% receive message from "elevator"
		if bel( received(A, atFloor(Level)) ) then insert(people(3)) + (elevator).sendonce((people(3))).
		if bel( received(A, atFloor(Level)) ) then insert(people(3)) + (elevator_0).sendonce((people(3))).
		if bel( received(A, atFloor(Level)) ) then insert(people(3)) + (elevator_1).sendonce((people(3))).
	}
}
